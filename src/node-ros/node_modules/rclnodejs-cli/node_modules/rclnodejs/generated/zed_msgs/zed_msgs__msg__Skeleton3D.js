// This file is automatically generated by Intel rclnodejs
//
// *** DO NOT EDIT directly
//
'use strict';
const ref = require('@rclnodejs/ref-napi');
const StructType = require('@rclnodejs/ref-struct-di')(ref);
const ArrayType = require('@rclnodejs/ref-array-di')(ref);
const primitiveTypes = require('../../rosidl_gen/primitive_types.js');
const deallocator = require('../../rosidl_gen/deallocator.js');
const translator = require('../../rosidl_gen/message_translator.js');
const Keypoint3DWrapper = require('../../generated/zed_msgs/zed_msgs__msg__Keypoint3D.js');
const Skeleton3DRefStruct = StructType({
  keypoints: ArrayType(Keypoint3DWrapper.refObjectType, 70),
});
const Skeleton3DRefArray = ArrayType(Skeleton3DRefStruct);
const Skeleton3DRefStructArray = StructType({
  data: Skeleton3DRefArray,
  size: ref.types.size_t,
  capacity: ref.types.size_t
});
// Define the wrapper class.
class Skeleton3DWrapper {
  constructor(other) {
    this._wrapperFields = {};
    if (typeof other === 'object' && other._refObject) {
      this._refObject = new Skeleton3DRefStruct(other._refObject.toObject());
      this._wrapperFields.keypoints = Keypoint3DWrapper.createArray();
      this._wrapperFields.keypoints.copy(other._wrapperFields.keypoints);
    } else if (typeof other !== 'undefined') {
      this._initMembers();
      translator.constructFromPlanObject(this, other);
    } else {
      this._initMembers();
    }
    this.freeze();
  }
  _initMembers() {
    this._refObject = new Skeleton3DRefStruct();
    this._wrapperFields.keypoints = Keypoint3DWrapper.createArray();
  }
  static createFromRefObject(refObject) {
    let self = new Skeleton3DWrapper();
    self.copyRefObject(refObject);
    return self;
  }
  static createArray() {
    return new Skeleton3DArrayWrapper;
  }
  static get ArrayType() {
    return Skeleton3DArrayWrapper;
  }
  static get refObjectArrayType() {
    return Skeleton3DRefStructArray
  }
  static get refObjectType() {
    return Skeleton3DRefStruct;
  }
  toRawROS() {
    this.freeze(true);
    return this._refObject.ref();
  }
  freeze(own = false, checkConsistency = false) {
    if (checkConsistency) {
    }
    for (let i = 0; i < this._wrapperFields.keypoints.data.length; i++) {
      this._refObject.keypoints[i] = this._wrapperFields.keypoints.data[i].freeze(own, checkConsistency);
      this._refObject.keypoints[i] = this._wrapperFields.keypoints.data[i].refObject;
    }
  }
  serialize() {
    this.freeze(false, true);
    return this._refObject.ref();
  }
  deserialize(refObject) {
    this._refObject.keypoints = refObject.keypoints;
    this._wrapperFields.keypoints.size = 70
    for (let i = 0; i < 70; i++) {
      this._wrapperFields.keypoints.data[i].copyRefObject(refObject.keypoints[i]);
    }
  }
  toPlainObject(enableTypedArray) {
    return translator.toPlainObject(this, enableTypedArray);
  }
  static freeStruct(refObject) {
    for (let i = 0; i < 70; i++) {
      Keypoint3DWrapper.freeStruct(refObject.keypoints[i]);
    }
  }
  static destoryRawROS(msg) {
    Skeleton3DWrapper.freeStruct(msg.refObject);
  }
  static type() {
    return {pkgName: 'zed_msgs', subFolder: 'msg', interfaceName: 'Skeleton3D'};
  }
  static isPrimitive() {
    return false;
  }
  static get isROSArray() {
    return false;
  }
  get refObject() {
    return this._refObject;
  }
  get keypoints() {
    return this._wrapperFields.keypoints;
  }
  set keypoints(value) {
    if (value.length !== 70) {
      throw new RangeError('The length of the array must be 70.');
    }
    this._wrapperFields.keypoints.fill(value);
  }
  copyRefObject(refObject) {
    this._refObject = new Skeleton3DRefStruct(refObject.toObject());
    this._refObject.keypoints = refObject.keypoints;
    this._wrapperFields.keypoints.size = 70
    for (let i = 0; i < 70; i++) {
      this._wrapperFields.keypoints.data[i].copyRefObject(refObject.keypoints[i]);
    }
  }
  copy(other) {
    this._refObject = new Skeleton3DRefStruct(other._refObject.toObject());
    this._wrapperFields.keypoints.copy(other._wrapperFields.keypoints);
  }
  static get classType() {
    return Skeleton3DWrapper;
  }
  static get ROSMessageDef() {
    return {"constants":[],"fields":[{"name":"keypoints","type":{"isArray":true,"arraySize":70,"isUpperBound":false,"isDynamicArray":false,"isFixedSizeArray":true,"pkgName":"zed_msgs","type":"Keypoint3D","stringUpperBound":null,"isPrimitiveType":false},"default_value":null}],"baseType":{"pkgName":"zed_msgs","type":"Skeleton3D","stringUpperBound":null,"isPrimitiveType":false},"msgName":"Skeleton3D"};
  }
  hasMember(name) {
    let memberNames = ["keypoints"];
    return memberNames.indexOf(name) !== -1;
  }
}
// Define the wrapper of array class.
class Skeleton3DArrayWrapper {
  constructor(size = 0) {
    this._resize(size);
  }
  toRawROS() {
    return this._refObject.ref();
  }
  fill(values) {
    const length = values.length;
    this._resize(length);
    values.forEach((value, index) => {
      if (value instanceof Skeleton3DWrapper) {
        this._wrappers[index].copy(value);
      } else {
        this._wrappers[index] = new Skeleton3DWrapper(value);
      }
    });
  }
  // Put all data currently stored in `this._wrappers` into `this._refObject`
  freeze(own) {
    this._wrappers.forEach((wrapper, index) => {
      wrapper.freeze(own);
      this._refArray[index] = wrapper.refObject;
    });
    this._refObject.size = this._wrappers.length;
    this._refObject.capacity = this._wrappers.length;
    if (this._refObject.capacity === 0) {
      this._refObject.data = null
    } else {
    this._refObject.data = this._refArray.buffer;
    }
  }
  get refObject() {
    return this._refObject;
  }
  get data() {
    return this._wrappers;
  }
  get size() {
    return this._wrappers.length;
  }
  set size(value) {
    if (typeof value != 'number') {
      throw new TypeError('Invalid argument: should provide a number to Skeleton3DArrayWrapper.size setter');
      return;
    }
    return this._resize(value);
  }
  get capacity() {
    return this._wrappers.length;
  }
  set capacity(value) {
    if (typeof value != 'number') {
      throw new TypeError('Invalid argument: should provide a number to Skeleton3DArrayWrapper.capacity setter');
    }
    return this._resize(value);
  }
  get refObject() {
    return this._refObject;
  }
  _resize(size) {
    if (size < 0) {
      throw new RangeError('Invalid argument: should provide a positive number');
      return;
    }
    this._refArray = new Skeleton3DRefArray(size);
    this._refObject = new Skeleton3DRefStructArray();
    this._refObject.size = size;
    this._refObject.capacity = size;
    this._wrappers = new Array();
    for (let i = 0; i < size; i++) {
      this._wrappers.push(new Skeleton3DWrapper());
    }
  }
  // Copy all data from `this._refObject` into `this._wrappers`
  copyRefObject(refObject) {
    this._refObject = refObject;
    let refObjectArray = this._refObject.data;
    refObjectArray.length = this._refObject.size;
    this._resize(this._refObject.size);
    for (let index = 0; index < this._refObject.size; index++) {
      this._wrappers[index].copyRefObject(refObjectArray[index]);
    }
  }
  copy(other) {
    if (! (other instanceof Skeleton3DArrayWrapper)) {
      throw new TypeError('Invalid argument: should provide "Skeleton3DArrayWrapper".');
    }
    this._resize(other.size);
    // Array deep copy
    other._wrappers.forEach((wrapper, index) => {
      this._wrappers[index].copy(wrapper);
    });
  }
  static freeArray(refObject) {
    let refObjectArray = refObject.data;
    refObjectArray.length = refObject.size;
    for (let index = 0; index < refObject.size; index++) {
      Skeleton3DWrapper.freeStruct(refObjectArray[index]);
    }
  }
  static get elementType() {
    return Skeleton3DWrapper;
  }
  static get isROSArray() {
    return true;
  }
  static get useTypedArray() {
    return false;
  }
  get classType() {
    return Skeleton3DArrayWrapper;
  }
}
module.exports = Skeleton3DWrapper;
/*
 * The following is the original spec object coming from parser:
{
  "constants": [],
  "fields": [
    {
      "name": "keypoints",
      "type": {
        "isArray": true,
        "arraySize": 70,
        "isUpperBound": false,
        "isDynamicArray": false,
        "isFixedSizeArray": true,
        "pkgName": "zed_msgs",
        "type": "Keypoint3D",
        "stringUpperBound": null,
        "isPrimitiveType": false
      },
      "default_value": null
    }
  ],
  "baseType": {
    "pkgName": "zed_msgs",
    "type": "Skeleton3D",
    "stringUpperBound": null,
    "isPrimitiveType": false
  },
  "msgName": "Skeleton3D"
}
*/
