// This file is automatically generated by rclnodejs
//
// *** DO NOT EDIT directly
//
'use strict';
const ref = require('../../third_party/ref-napi');
const StructType = require('@rclnodejs/ref-struct-di')(ref);
const ArrayType = require('@rclnodejs/ref-array-di')(ref);
const primitiveTypes = require('../../rosidl_gen/primitive_types.js');
const deallocator = require('../../rosidl_gen/deallocator.js');
const translator = require('../../rosidl_gen/message_translator.js');
const Keypoint2DiWrapper = require('../../generated/zed_msgs/zed_msgs__msg__Keypoint2Di.js');
const BoundingBox2DiRefStruct = StructType({
  corners: ArrayType(Keypoint2DiWrapper.refObjectType, 4),
});
const BoundingBox2DiRefArray = ArrayType(BoundingBox2DiRefStruct);
const BoundingBox2DiRefStructArray = StructType({
  data: BoundingBox2DiRefArray,
  size: ref.types.size_t,
  capacity: ref.types.size_t
});
class BoundingBox2DiWrapper {
  constructor(other) {
    this._initialize();
    this._setDefaults();
     if (typeof other === 'object' && other._refObject) {
      this._refObject = new BoundingBox2DiRefStruct(other._refObject.toObject());
      this._wrapperFields.corners = Keypoint2DiWrapper.createArray();
      this._wrapperFields.corners.copy(other._wrapperFields.corners);
    } else if (typeof other !== 'undefined') {
      translator.constructFromPlanObject(this, other);
    }
    this.freeze(/*own=*/false);
  }
  _setDefaults() {
  }
  _initialize() {
    this._wrapperFields = {};
    this._refObject = new BoundingBox2DiRefStruct();
    this._wrapperFields.corners = Keypoint2DiWrapper.createArray();
  }
  static createFromRefObject(refObject) {
    let self = new BoundingBox2DiWrapper();
    self.copyRefObject(refObject);
    return self;
  }
  static createArray() {
    return new BoundingBox2DiArrayWrapper();
  }
  static get ArrayType() {
    return BoundingBox2DiArrayWrapper;
  }
  static get refObjectArrayType() {
    return BoundingBox2DiRefStructArray;
  }
  static get refObjectType() {
    return BoundingBox2DiRefStruct;
  }
  toRawROS() {
    this.freeze(/*own=*/true);
    return this._refObject.ref();
  }
  freeze(own) {
    for (let i = 0; i < 4; i++) {
      if (this._wrapperFields.corners.data[i]) {
        this._wrapperFields.corners.data[i].freeze(own);
        this._refObject.corners[i] = this._wrapperFields.corners.data[i].refObject;
      }
    }
  }
  serialize() {
    this.freeze(/*own=*/false);
    return this._refObject.ref();
  }
  deserialize(refObject) {
    this._refObject.corners = refObject.corners;
    this._wrapperFields.corners.size = 4
    for (let i = 0; i < 4; i++) {
      this._wrapperFields.corners.data[i].copyRefObject(refObject.corners[i]);
    }
  }
  toPlainObject(enableTypedArray) {
    return translator.toPlainObject(this, enableTypedArray);
  }
  static freeStruct(refObject) {
    for (let i = 0; i < 4; i++) {
      Keypoint2DiWrapper.freeStruct(refObject.corners[i]);
    }
  }
  static destroyRawROS(msg) {
    BoundingBox2DiWrapper.freeStruct(msg.refObject);
  }
  static type() {
    return {pkgName: 'zed_msgs', subFolder: 'msg', interfaceName: 'BoundingBox2Di'};
  }
  static isPrimitive() {
    return false;
  }
  static get isROSArray() {
    return false;
  }
  get refObject() {
    return this._refObject;
  }
  get corners() {
    return this._wrapperFields.corners;
  }
  set corners(value) {
    if (value.length !== 4) {
      throw new RangeError('The length of the array must be 4.');
    }
    this._wrapperFields.corners.fill(value);
  }
  copyRefObject(refObject) {
    this._refObject = new BoundingBox2DiRefStruct(refObject.toObject());
    this._refObject.corners = refObject.corners;
    this._wrapperFields.corners.size = 4
    for (let i = 0; i < 4; i++) {
      this._wrapperFields.corners.data[i].copyRefObject(refObject.corners[i]);
    }
  }
  copy(other) {
    this._refObject = new BoundingBox2DiRefStruct(other._refObject.toObject());
    this._wrapperFields.corners.copy(other._wrapperFields.corners);
  }
  static get classType() {
    return BoundingBox2DiWrapper;
  }
  static get ROSMessageDef() {
    return {"constants":[],"fields":[{"name":"corners","type":{"isArray":true,"arraySize":4,"isUpperBound":false,"isDynamicArray":false,"isFixedSizeArray":true,"pkgName":"zed_msgs","type":"Keypoint2Di","stringUpperBound":null,"isPrimitiveType":false},"default_value":null}],"baseType":{"pkgName":"zed_msgs","type":"BoundingBox2Di","stringUpperBound":null,"isPrimitiveType":false},"msgName":"BoundingBox2Di"};
  }
  hasMember(name) {
    let memberNames = ["corners"];
    return memberNames.indexOf(name) !== -1;
  }
}
class BoundingBox2DiArrayWrapper {
  constructor(size = 0) {
    this._resize(size);
  }
  toRawROS() {
    return this._refObject.ref();
  }
  fill(values) {
    const length = values.length;
    this._resize(length);
    values.forEach((value, index) => {
      if (value instanceof BoundingBox2DiWrapper) {
        this._wrappers[index].copy(value);
      } else {
        this._wrappers[index] = new BoundingBox2DiWrapper(value);
      }
    });
  }
  freeze(own) {
    this._wrappers.forEach((wrapper, index) => {
      wrapper.freeze(own);
      this._refArray[index] = wrapper.refObject;
    });
    this._refObject.size = this._wrappers.length;
    this._refObject.capacity = this._wrappers.length;
    if (this._refObject.capacity === 0) {
      this._refObject.data = null
    } else {
      this._refObject.data = this._refArray.buffer;
    }
  }
  get refObject() {
    return this._refObject;
  }
  get data() {
    return this._wrappers;
  }
  get size() {
    return this._wrappers.length;
  }
  set size(value) {
    if (typeof value != 'number') {
      throw new TypeError('Invalid argument: should provide a number to BoundingBox2DiArrayWrapper.size setter');
    }
    return this._resize(value);
  }
  get capacity() {
    return this._wrappers.length;
  }
  set capacity(value) {
    if (typeof value != 'number') {
      throw new TypeError('Invalid argument: should provide a number to BoundingBox2DiArrayWrapper.capacity setter');
    }
    return this._resize(value);
  }
  _resize(size) {
    if (size < 0) {
      throw new RangeError('Invalid argument: should provide a positive number');
    }
    this._refArray = new BoundingBox2DiRefArray(size);
    this._refObject = new BoundingBox2DiRefStructArray();
    this._refObject.size = size;
    this._refObject.capacity = size;
    this._wrappers = new Array();
    for (let i = 0; i < size; i++) {
      this._wrappers.push(new BoundingBox2DiWrapper());
    }
  }
  copyRefObject(refObject) {
    this._refObject = refObject;
    let refObjectArray = this._refObject.data;
    refObjectArray.length = this._refObject.size;
    this._resize(this._refObject.size);
    for (let index = 0; index < this._refObject.size; index++) {
      this._wrappers[index].copyRefObject(refObjectArray[index]);
    }
  }
  copy(other) {
    if (! (other instanceof BoundingBox2DiArrayWrapper)) {
      throw new TypeError('Invalid argument: should provide "BoundingBox2DiArrayWrapper".');
    }
    this._resize(other.size);
    other._wrappers.forEach((wrapper, index) => {
      this._wrappers[index].copy(wrapper);
    });
  }
  static freeArray(refObject) {
    let refObjectArray = refObject.data;
    refObjectArray.length = refObject.size;
    for (let index = 0; index < refObject.size; index++) {
      BoundingBox2DiWrapper.freeStruct(refObjectArray[index]);
    }
  }
  static get elementType() {
    return BoundingBox2DiWrapper;
  }
  static get isROSArray() {
    return true;
  }
  static get useTypedArray() {
    return false;
  }
  get classType() {
    return BoundingBox2DiArrayWrapper;
  }
  toPlainObject(enableTypedArray) {
    return translator.toPlainObject(this, enableTypedArray);
  }
}
module.exports = BoundingBox2DiWrapper;
